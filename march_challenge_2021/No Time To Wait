#include<bits/stdc++.h>
//#include<conio>
using namespace std;
int main()
{
 //int t; cin>>t;
 int n,h,x;
 cin>>n>>h>>x;
 int a[n];
 for(int i=0;i<n;i++)
 {
  cin>>a[i];
 }
 sort(a,a+n);
 //cout<<a[n-1];
 if((a[n-1]+x)==h)
 {
  cout<<"YES";
 }
else
  cout<<"NO";
  return 0;
}

